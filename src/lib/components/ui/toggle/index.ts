import { Toggle as TogglePrimitive } from 'bits-ui';
import Toggle from './toggle.svelte';

const Root = Toggle;

export {
        Root,
        Toggle,
        TogglePrimitive,
        Root as ToggleRoot
};
